{
  "synapse_version": "0.3.0",
  "initialized_at": "2025-11-05T00:00:00Z",
  "project": {
    "name": "test-monorepo",
    "root_directory": "/home/user/synapse/tests/fixtures/monorepo-optimization"
  },
  "workflows": {
    "active_workflow": "feature-implementation",
    "applied_workflows": []
  },
  "settings": {
    "auto_backup": true,
    "strict_validation": true,
    "uv_required": false
  },
  "quality-config": {
    "mode": "monorepo",
    "optimization": {
      "check_affected_only": true,
      "detection_method": "uncommitted",
      "fallback_to_all": true,
      "force_check_projects": ["shared-utils"],
      "verbose_logging": false
    },
    "projects": {
      "backend": {
        "directory": "backend/",
        "projectType": "python",
        "commands": {
          "lint": "cd backend && echo 'linting backend'",
          "test": "cd backend && echo 'testing backend'"
        },
        "thresholds": {
          "lintLevel": "strict"
        }
      },
      "frontend": {
        "directory": "frontend/",
        "projectType": "node",
        "commands": {
          "lint": "cd frontend && echo 'linting frontend'",
          "test": "cd frontend && echo 'testing frontend'"
        },
        "thresholds": {
          "lintLevel": "flexible"
        }
      },
      "shared-utils": {
        "directory": "shared-utils/",
        "projectType": "python",
        "commands": {
          "lint": "cd shared-utils && echo 'linting shared-utils'",
          "test": "cd shared-utils && echo 'testing shared-utils'"
        },
        "thresholds": {
          "lintLevel": "strict"
        }
      }
    }
  }
}
