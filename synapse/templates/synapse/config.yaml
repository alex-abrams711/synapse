# Synapse Agent Workflow System Configuration
# Generated by Synapse v{{synapse_version}} on {{created_at}}

project_name: "{{project_name}}"
synapse_version: "{{synapse_version}}"
workflow_dir: "{{workflow_dir}}"
task_log_path: "task_log.json"

# Agent Configuration
agents:
  dev:
    enabled: true
    context_file: ".claude/agents/dev.md"
    custom_rules: []
  auditor:
    enabled: true
    context_file: ".claude/agents/auditor.md"
    custom_rules: []
  dispatcher:
    enabled: true
    context_file: ".claude/agents/dispatcher.md"
    custom_rules: []

# Claude Code Slash Commands
claude_commands:
  status:
    enabled: true
    command_file: ".claude/commands/status.md"
  workflow:
    enabled: true
    command_file: ".claude/commands/workflow.md"
  validate:
    enabled: true
    command_file: ".claude/commands/validate.md"
  agent:
    enabled: true
    command_file: ".claude/commands/agent.md"

# Timestamps
created_at: "{{created_at}}"
last_updated: "{{created_at}}"